SELECT
    post_id,
    user_id,
    parent_post_id,
    accepted_answer_id,
    creation_date,
    score,
    view_count,
    answer_count,
    comment_count,
    post_type
FROM my_workspace.silver.posts